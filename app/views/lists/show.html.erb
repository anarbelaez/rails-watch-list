<%= link_to "Add new bookmark", new_list_bookmark_path(@list), class: "btn btn-info"  %>
<br>

<div data-controller="insert-in-list">
  <div data-insert-in-list-target="items">
    <% if @list.bookmarks.blank? %>
      Be the first to leave a review for <%= @list.nombre %>
    <% else %>
    <div class="row">
      <% @list.bookmarks.each do |bookmark| %>
        <div class="col-md-2">
          <%= render "bookmarks/bookmark", bookmark: bookmark %>
        </div>
      <% end %>
    </div>
    <% end %>
  </div>

  <%= render "bookmarks/form", list: @list, bookmark: @bookmark %>
</div>

<%= link_to "Back to lists", lists_path %>
